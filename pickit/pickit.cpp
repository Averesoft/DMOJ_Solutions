#include <bits/stdc++.h>
using namespace std;
const int mm = 200 + 5;
int n, k[mm];
int main() {
    while(true) {
        cin >> n;
        if (n == 0) break;
        for (int i = 1; i <= n; i++) {
            cin >> k[i];
        }
        
    }
}