#include <bits/stdc++.h>
using namespace std;
void solve() {
    int n, k; cin >> n >> k;
    for (int i = 0, a, b; i < n - 1; i++) {
        cin >> a >> b;
    }
    for (int i = 1; i <= n; i++) {
        
    }
}
int main() {
    int t; cin >> t;
    while(t--) {
        solve();
    }
}