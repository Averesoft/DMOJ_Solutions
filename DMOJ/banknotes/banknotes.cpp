#include <bits/stdc++.h>
using namespace std;
const int mm = 200 + 5;
int n, b[mm], c[mm], k;
int dp[mm][mm];
int main() {
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> b[i];
    }
    for (int i = 1; i <= n; i++) {
        cin >> c[i];
    }
    cin >> k;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            
        }
    }
    cout << dp[n][n] << '\n';
}