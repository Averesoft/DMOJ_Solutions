#include <bits/stdc++.h>
using namespace std;
const int mm = 15 + 5;
int N, M, W, w[mm];
int main() {
    cin >> N >> M;
    for (int i = 0, X, Y; i < M; i++) {
        cin >> X >> Y;
    }
    cin >> W;
    for (int i = 0; i < W; i++) {
        cin >> w[i];
    }
}