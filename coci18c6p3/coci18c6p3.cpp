#include <bits/stdc++.h>
using namespace std;
const int mm = 500 + 5;
int n, m, k, p[mm], b[mm];
int main() {
    cin >> n >> m >> k;
    for (int i = 0; i < n; i++) {
        cin >> p[i];
    }
    for (int i = 0; i < m + 1; i++) {
        cin >> b[i];
    }
}