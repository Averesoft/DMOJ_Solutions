#include <bits/stdc++.h>
using namespace std;
int N, D;
int main() {
    cin >> N >> D;
    for (int i = 1, a, b, c; i < N; i++) {
        cin >> a >> b >> c;
    }
    for (int i = 1, x, y; i <= D; i++) {
        cin >> x >> y;
    }
}