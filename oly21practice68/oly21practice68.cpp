#include <bits/stdc++.h>
using namespace std;
const int mm = 1e6 + 5;
int N, X[mm], P[mm], C[mm];
int main() {
    cin >> N;
    for (int i = 1; i <= N; i++) {
        cin >> X[i] >> P[i] >> C[i];
    }
}