#include <bits/stdc++.h>
using namespace std;
const int mm = 200000 + 5;
int k, n, z[mm];
int main() {
    cin >> k >> n;
    for (int i = 1; i <= k; i++) {
        cin >> z[i];
    }
    for (int i = 1, a, b, d; i < n; i++) {
        cin >> a >> b >> d;
    }
}