#include <bits/stdc++.h>
using namespace std;
const int mm = 100000 + 5;
int N, K, A[mm], B[mm];
int main() {
    cin >> N >> K;
    for (int i = 1; i <= N; i++) {
        cin >> A[i];
    }
    for (int i = 1; i <= N; i++) {
        cin >> B[i];
    }
}