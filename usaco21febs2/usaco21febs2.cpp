#include <bits/stdc++.h>
using namespace std;
const int mm = 65536 + 5;
int n, k;
int ancestors[mm];
int main() {
    cin >> n >> k;
    for (int i = 0;i < n; i++) {
        cin >> ancestors[i];
    }
    int time = 0;
    sort(ancestors, ancestors+n);
    int idx = 0, counter = 0;
    while(counter < k) {
        
    }
    
}