#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int mm = 6 + 5;
int N, M[mm];
vector<int> adj[mm];
int main() {
    cin >> N;
    for (int i = 1; i <= N; i++) {
        for (int j = 1; j <= N; j++) {
            cin >> M[i]; 
                
        }
    }
}