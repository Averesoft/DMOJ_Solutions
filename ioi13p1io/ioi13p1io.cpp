#include <bits/stdc++.h>
using namespace std;
const int mm = 1e5 + 5;
int n, m, a[mm], b[mm], t[mm], l;
int main() {
    cin >> n >> m >> l;
    for (int i = 0; i < m; i++) {
        cin >> a[i] >> b [i] >> t[i];
    }
    
}