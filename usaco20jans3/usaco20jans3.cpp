#include <bits/stdc++.h>
using namespace std;
const int mm = 1e5 + 5;
int n, m, p[mm];
int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> p[i];
    }
    for (int i = 1, a, b, w; i <= m; i++) {
        cin >> a >> b >> w;
    }
}