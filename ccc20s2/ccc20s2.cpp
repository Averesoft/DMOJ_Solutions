#include <bits/stdc++.h>
using namespace std;
const int mm = 1000 + 5;
int M, N, grid[mm][mm];
int main() {
    cin >> M >> N;
    for (int i = 0; i < M; i++) {
        for (int j = 0; j < N; j++) {
            cin >> grid[i][j];
        }
    }
    
}