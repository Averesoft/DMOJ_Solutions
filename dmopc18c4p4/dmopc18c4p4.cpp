#include <bits/stdc++.h>
using namespace std;
const int mm = 200000 + 5;
int N, Q, A[mm];
int main() {
    cin >> N >> Q;
    for (int i = 1; i <= N; i++) {
        cin >> A[i];
    }
    for (int i = 1, l, r, k; i <= Q; i++) {
        cin >> l >> r >> k;
    }
}